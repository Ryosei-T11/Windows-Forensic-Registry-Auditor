receivers:
  otlp:
    protocols:
      grpc: {}
      http: {}

exporters:
  logging:
    logLevel: info
  jaeger:
    endpoint: "http://jaeger:14268/api/traces"
  otlp:
    endpoint: ""

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [logging, jaeger]
